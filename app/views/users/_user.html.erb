<div class="card d-grid gap-2 col-8 mx-auto text-center">
  <%= letter_avatar_tag(@user.surname, 300, class: 'card-img-top') %>
  <div class="card-body">
    <h5 class="card-title"><%= @user.full_name %></h5>
    <p class="card-text"><%= @user.email %></p>
  </div>
</div>
